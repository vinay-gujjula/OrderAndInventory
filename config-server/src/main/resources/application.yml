server:
  port: 8888

spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/vinay-gujjula/microservices-config.git
          default-label: main
          clone-on-start: true