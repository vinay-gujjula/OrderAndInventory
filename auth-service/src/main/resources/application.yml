server:
  port: 8081

spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://capstone-db.cng4m02w2jrz.ap-southeast-2.rds.amazonaws.com:3306/capstone
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: admin
    password: Vinni014291
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://172.31.27.189/eureka/
    register-with-eureka: true
    fetch-registry: true

jwt:
  secret: mySecretKey

logging:
  level:
    org.springframework.security: DEBUG
    com.example.authservice: DEBUG